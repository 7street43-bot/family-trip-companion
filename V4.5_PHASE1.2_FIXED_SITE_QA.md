# V4.5 Phase 1.2｜Fixed Site + OAuth Redirect Guard QA

Runtime: `4.5.0-phase1.2`  
Production Site: `https://comfy-heliotrope-475c71.netlify.app`  
Netlify owner account: `kumohsun11@gmail.com`

## Purpose

Lock Cloud Sync/OAuth to the user's current permanent Netlify production site and reject login from stale/old Netlify origins.

## Contracts

- `APP_SITE_URL` is exposed only as a non-secret production origin.
- Google OAuth sign-in is blocked when `location.origin` differs from the fixed production origin.
- OAuth redirect target must remain on the fixed production origin.
- Cloud Sync Gate includes a production-site origin PASS/FAIL row.
- Supabase publishable key remains public-client only; no service role / `sb_secret_` key is shipped.
- Existing IndexedDB data and sync schema are unchanged.

## External LIVE gates still required

1. In Netlify site `comfy-heliotrope-475c71` under the `kumohsun11@gmail.com` account, configure:
   - `GOOGLE_PLACES_API_KEY`
   - `SUPABASE_URL`
   - `SUPABASE_PUBLISHABLE_KEY`
   - `APP_SITE_URL=https://comfy-heliotrope-475c71.netlify.app`
2. Google Cloud OAuth Web Client:
   - Authorized JavaScript origin: `https://comfy-heliotrope-475c71.netlify.app`
   - Authorized redirect URI: `https://iaecgwitsxghsovdkotw.supabase.co/auth/v1/callback`
3. Supabase Auth URL Configuration:
   - Site URL: `https://comfy-heliotrope-475c71.netlify.app`
   - Redirect allow-list should include the same production origin/path used by the app.
4. GitHub repo `7street43-bot/baby-trip` must be connected to this exact Netlify site for automatic deployment.
5. Cross-device LIVE roundtrip must PASS before enabling Cloud Sync as production-ready.
