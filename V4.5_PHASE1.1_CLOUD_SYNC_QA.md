# V4.5 Phase 1.1｜Cloud Sync Ready QA

Runtime: `4.5.0-phase1.1`  
Base: V4.4 TEST3.4 + Phase 1 Supabase foundation

## Final local gate

**LOCAL PHASE 1.1 GATE: PASS**

- Static / integration checks: **36/36 PASS**
- Optimistic-concurrency model fuzz: **50,000 scenarios PASS**
- Final ZIP extract-and-replay: **PASS**
- `public/app.js`, `public/cloud-sync.js`, `public/sw.js`, all Netlify Functions syntax: **PASS**
- Public JSON parse: **PASS**

## Cloud Sync client contracts

- Google OAuth entry point implemented.
- Same Supabase session can be reused across browser/PWA sessions.
- Personal Family Workspace bootstrap implemented.
- Existing IndexedDB remains the local/offline cache.
- Before first cloud merge, a full local snapshot is stored in IndexedDB metadata (`preCloudSyncBackup`).
- First sync uses union behavior rather than blindly replacing one device with another.
- Delta pull uses server-monotonic `change_seq`.
- Writes use `family_sync_push` with expected version (optimistic locking).
- A version conflict stops instead of silently overwriting another device.
- Local deletions become server tombstones.
- Realtime subscription is wired for subsequent changes.
- `journals` is reserved at the server layer for Phase 2; journal UI is not part of Phase 1.1.

## Supabase live foundation read-back

Project: `iaecgwitsxghsovdkotw`

Applied migrations:

1. `20260831024944 v45_phase1_cloud_sync_foundation`
2. `20260831025022 v45_phase1_private_rls_helpers`
3. `20260831025058 v45_phase1_fk_indexes`

Verified:

- required tables: **3/3**
- RLS: **enabled**
- Realtime publication: **enabled**
- public authenticated RPCs: **2**
- membership helpers moved to private schema
- authenticated direct `INSERT / UPDATE / DELETE`: **revoked**
- authenticated read: **allowed only through RLS**

Security advisor has two intentional WARNs for the two authenticated `SECURITY DEFINER` RPCs. Both RPCs enforce authentication and access/version checks. No secret/service-role key is exposed to the frontend.

Performance advisor has only INFO notices for currently-unused indexes, expected before real synchronization traffic.

## Netlify client configuration

`/api/cloud-config` exposes only:

- Supabase project URL
- Supabase **publishable** key

It rejects service-role / `sb_secret_` credentials. Environment-variable overrides remain available.

## Live boundary — NOT yet certified

Local QA cannot prove the following until external configuration is completed:

1. Google OAuth provider is configured in Google Cloud + Supabase Auth.
2. Supabase Site URL / redirect allow-list matches the current fixed Netlify production site.
3. GitHub repository is connected to the fixed Netlify site with automatic deploys.
4. Real cross-device roundtrip passes: desktop write → iPhone sees it, and iPhone write → desktop sees it.
5. Conflict LIVE test passes on two devices.

Therefore:

- `LOCAL PHASE 1.1 = PASS`
- `GOOGLE LOGIN LIVE = PENDING`
- `GITHUB → NETLIFY AUTO DEPLOY = PENDING`
- `CROSS-DEVICE LIVE SYNC = PENDING`
- `V4.5 PRODUCTION READY = NO`

## Required Google OAuth callback

Google authorized redirect URI for Supabase:

`https://iaecgwitsxghsovdkotw.supabase.co/auth/v1/callback`

The Google authorized JavaScript origin and Supabase Site URL must use the user's **current fixed Netlify site origin**. Do not use an older Netlify URL from prior deployments.
