# V4.5 Phase 1 — Cloud Sync Foundation QA

Status: **DATABASE FOUNDATION PASS / LOGIN GATE PENDING / LIVE SYNC NOT ENABLED**

## Applied Supabase migrations
- `v45_phase1_cloud_sync_foundation`
- `v45_phase1_private_rls_helpers`
- `v45_phase1_fk_indexes`

## Read-back
- 3 required public tables: PASS
- 3 RLS SELECT policies: PASS
- RLS on `family_sync_records`: PASS
- Realtime publication on `family_sync_records`: PASS
- 3 core indexes + FK covering indexes: PASS
- 2 public RPCs: PASS
- 2 private membership helpers: PASS

## Write boundary
Authenticated browser role:
- SELECT `family_sync_records`: YES (RLS constrained)
- INSERT: NO
- UPDATE: NO
- DELETE: NO
- `ensure_personal_family_workspace()` RPC: YES
- `family_sync_push()` RPC: YES

This prevents direct browser writes from bypassing optimistic concurrency.

## Sync contract
- Server monotonic `change_seq` delta cursor
- Per-record `version`
- Optimistic locking / conflict response
- Soft-delete tombstones
- Workspace membership isolation
- Realtime-ready
- Reserved `journals` store for V4.5 journal feature

## Security advisor
Two INFO/WARN-class findings remain intentionally: the two authenticated `SECURITY DEFINER` RPCs are deliberately exposed endpoints. Both perform authentication and access validation, and table mutation grants are revoked.

## Frontend integration
- `public/cloud-sync.js` added as dormant adapter.
- `/api/cloud-config` Netlify Function added.
- No automatic login or sync occurs yet.
- Existing IndexedDB remains authoritative until Google Login Gate is completed and sync is explicitly enabled.

## Pending Gate
1. Configure Supabase Google OAuth provider + redirect URL.
2. Configure fixed Netlify site env vars: `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`.
3. Create/connect GitHub repository and bind Netlify continuous deployment.
4. Run first two-device LIVE sync acceptance before enabling cloud as source of truth.
